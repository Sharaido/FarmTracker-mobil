<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Test.Views.ItemDetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Test.ViewModels;assembly=Test"
    Title="{Binding Title}"
    x:DataType="viewModels:ItemDetailViewModel">

    <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding EditItemCommand}" Text="Save" />
        <ToolbarItem Command="{Binding DeleteItemCommand}" Text="Delete" />
    </ContentPage.ToolbarItems>

    <StackLayout Padding="15" Spacing="20">
        <Label FontSize="Medium" Text="Text:" />
        <Entry
            x:Name="EntryText"
            IsPassword="False"
            Text="{Binding Text}" />

        <Label FontSize="Medium" Text="Description:" />
        <Entry
            x:Name="EntryDesc"
            IsPassword="False"
            Text="{Binding Description}" />

    </StackLayout>


</ContentPage>